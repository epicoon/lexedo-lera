name: lexedo/lera

autoload:
  psr-4:
    lexedo\lera\: ''

service:
  class: lexedo\lera\Service

  routes:
    letters: {plugin: letters}
    bricks: {plugin: bricks}
    enabc: {plugin: enabc}
    bank: {plugin: bank}
    solar: {plugin: solar}

  plugins: plugin
  models: model
